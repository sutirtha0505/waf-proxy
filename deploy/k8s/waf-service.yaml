apiVersion: v1
kind: Service
metadata:
  name: smart-waf
spec:
  selector:
    app: smart-waf
  ports:
    - name: proxy
      port: 8080
      targetPort: 8080
    - name: dashboard
      port: 9090
      targetPort: 9090
