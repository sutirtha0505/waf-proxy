# Attack Vectors

The initial registry contains 37 attack vectors in `pkg/models/attack_vectors.go`, ending with `http_parameter_pollution` as code `37`. Runtime additions are persisted through the repository.
